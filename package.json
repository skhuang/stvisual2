{
  "name": "stvisual",
  "version": "1.0.0",
  "description": "軟體測試方法視覺化",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "python3 -m http.server 4174 --protocol HTTP/1.1",
    "inject-env": "node scripts/inject-env.mjs",
    "build:quiz": "node scripts/build-quiz.mjs",
    "build:labs": "node scripts/build-labs.mjs",
    "build:slide-decks": "node scripts/build-slide-decks.mjs",
    "build:standalone": "node scripts/build-standalone.mjs",
    "pages:prepare": "npm run build:quiz && npm run build:labs && npm run build:slide-decks && npm run inject-env && npm run build:standalone && node scripts/prepare-pages.mjs",
    "pages:build": "npm run build:quiz && npm run build:labs && npm run build:slide-decks && npm run inject-env && vite build",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:browser": "playwright test",
    "test:browser:headed": "playwright test --headed",
    "print:test-plan": "pandoc MANUAL_TEST_PLAN.md -o MANUAL_TEST_PLAN.html --standalone --metadata title='Manual Test Plan' --css=scripts/print.css && open MANUAL_TEST_PLAN.html",
    "print:test-plan:pdf": "pandoc MANUAL_TEST_PLAN.md -o MANUAL_TEST_PLAN.pdf --pdf-engine=xelatex -V geometry:margin=2cm -V mainfont='PingFang TC' -V CJKmainfont='PingFang TC' && open MANUAL_TEST_PLAN.pdf",
    "firestore:deploy": "firebase deploy --only firestore:rules,firestore:indexes",
    "firestore:rules": "firebase deploy --only firestore:rules"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@testing-library/jest-dom": "^6.4.0",
    "esbuild": "^0.25.3",
    "fast-xml-parser": "^5.11.0",
    "jsdom": "^24.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.11",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "firebase": "^12.12.0"
  }
}
